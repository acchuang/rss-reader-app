# RSS Reader Web

Frontend reader surface for the live Railway backend.

Development:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
```

Environment:

- `VITE_API_BASE_URL`: API origin for the deployed backend

This app is intentionally static and reads from the live API that is already deployed on Railway.
